<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
            color: #333;
        }

        .table-container {
            height: 600px; /* Increased height */
            overflow-y: auto;
            position: relative;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #ffffff;
            position: relative;
        }

        th, td {
            padding: 15px 20px;
            border: 1px solid #ccc; /* Added border */
        }

        th {
            background-color: #e1e1e1;
            text-transform: uppercase;
            font-weight: bold;
            position: sticky;
            top: 0;
            z-index: 1;
        }

        tr:hover {
            background-color: #f9f9f9;
        }

        td {
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .has-data {
            background-color: #d9f9d9; /* Different color for cells with data */
        }

        .nested-table {
            width: 100%;
            border-collapse: collapse;
            background-color: #f1f1f1;
            margin-top: 10px;
        }

        .nested-table th, .nested-table td {
            padding: 8px;
            border-bottom: 1px solid #ccc;
        }

        .nested-table th {
            background-color: #e1e1e1;
            text-transform: uppercase;
            font-weight: bold;
        }
    </style>
    <script>
        function toggleDetails(country) {
            const details = document.querySelectorAll(`.details-${country}`);
            details.forEach(detail => {
                if (detail.style.display === 'none' || !detail.style.display) {
                    detail.style.display = 'block';
                } else {
                    detail.style.display = 'none';
                }
            });
        }
    </script>
</head>
<body>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>Country</th>
                    <th>Airalo</th>
                    <th>Holafly</th>
                    <th>Getnomad</th>
                </tr>
            </thead>
            <tbody>
                <% for(let countryName in countries) { %>
                    <tr>
                        <td onclick="toggleDetails('<%= countryName %>')">
                            <%= countryName %>
                        </td>
                        <% countries[countryName].forEach((site, index) => { %>
                            <td class="<%= site.items.length > 0 ? 'has-data' : '' %>">
                                <div class="details-<%= countryName %>" style="display: none;">
                                    <table class="nested-table">
                                        <thead>
                                            <tr>
                                                <th>Data</th>
                                                <th>Duration</th>
                                                <th>Price</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% site.items.forEach(item => { %>
                                                <tr>
                                                    <td><%= item.data %></td>
                                                    <td><%= item.duration %></td>
                                                    <td>$<%= item.price %></td>
                                                </tr>
                                            <% }); %>
                                        </tbody>
                                    </table>
                                </div>
                            </td>
                        <% }); %>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
